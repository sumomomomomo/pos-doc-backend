package horse.sumomo.pos_doc_backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * The skeleton is HTTP-only and has no database configured. The JDBC datasource
 * auto-configuration is excluded in {@code application.yaml} (skeleton only;
 * TODO: remove when PostgreSQL and JPA are introduced).
 */
@SpringBootTest
class PosDocBackendApplicationTests {

	@Test
	void contextLoads() {
	}

}
